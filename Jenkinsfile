pipeline {
    agent any

    stages {
        stage('Build and Deploy') {
            steps {

                
                sh 'sudo docker build -t devxpace-test:3.0 .'
        }      
        }
        
    }
}